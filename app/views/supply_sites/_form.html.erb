<%= form_with(model: supply_site, local: true) do |form| %>
  <% if supply_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supply_site.errors.count, "error") %> prohibited this supply_site from being saved:</h2>

      <ul>
      <% supply_site.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :capacity %>
    <%= form.text_field :capacity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
