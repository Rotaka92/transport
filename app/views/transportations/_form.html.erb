<%= form_with(model: transportation, local: true) do |form| %>
  <% if transportation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transportation.errors.count, "error") %> prohibited this transportation from being saved:</h2>

      <ul>
      <% transportation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :supply_site_id %>
    <%= form.collection_select :supply_site_id, SupplySite.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :demand_site_id %>
    <%= form.collection_select :demand_site_id, DemandSite.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :cost_factor %>
    <%= form.text_field :cost_factor %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
